请在这个项目文件夹下，创建一个可视化UI，可打包为exe可执行文件，即点即用

界面第一行：文件夹选择输入框、文件夹选择按钮（输入框内容：characterLive项目地址，默认值：E:\mine\gitspace\characterLive）

界面第二行：文件夹选择输入框、文件夹选择按钮（输入框内容：singsong项目地址，默认值：C:\MiaoMiao\singsong）

界面第三行：文件夹选择输入框、文件夹选择按钮（输入框内容：so-vits-svc项目地址，默认值：C:\MiaoMiao\so-vits-svc）

界面第四行：文本输入框、删除按钮（输入框内容：歌名）

界面第五行：（多给点高度）一个只读终端，用于显示输出

## 功能实现：

- 在用户第一次点击删除按钮后，自动保存前三行地址（保存到当前补丁项目的配置文件中），下次打开应用时保持不变
- 在用户点击删除按钮后，自动在以下文件夹中**遍历**（逐层递归）查找**文件名含有**（包含即可，但必须完全匹配）歌名的文件

characterLive项目地址/songs
singsong项目地址/songs
singsong项目地址/output

- 终端显示应有的输出

-------------------------------------------------------------------------

- 界面第四行改为：文本输入框、精确删除按钮、删除按钮（输入框内容：歌名）

功能为：在用户点击删除按钮后，自动在以下文件夹中**遍历**（逐层递归）查找**文件名含有**（包含即可，但必须完全匹配）歌名的文件；在用户点击精确删除按钮后，自动在以下文件夹中**遍历**（逐层递归）查找**文件名为**（必须完全匹配）歌名的文件

- 在第四行下面，终端输出窗口的上面，增加一行，内容为：文件夹选择输入框、文件夹选择按钮、转移按钮（输入框内容：mp3文件存储地址，默认值：E:\mine\songs-for-mm）

功能为：

首先遍历mp3文件存储地址下的全部mp3文件，并将后缀名统一改为小写，然后，提取出全部的文件名（不带扩展名），逐个与 characterLive项目地址\songs\download 目录下的mp3文件名相比较，完全相同的跳过操作，若characterLive项目地址\songs\download 目录下的mp3文件中没有当前正在检测的文件名，则将当前mp3文件复制到characterLive项目地址\songs\download 目录下